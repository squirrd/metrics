---
# Agent config
metrics_api:
  endpoint:       "http://localhost:8080"
  organisation:   "metrics"
  bucket:         "metrics"
  measurement:    "system_metrics"
  # pwd/token:

metrics:
  interval: 1

